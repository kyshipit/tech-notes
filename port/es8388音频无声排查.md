# RK3588 ES8388 音频无声问题 — 完整排查总结

## 一、问题现象

- 硬件：ATK-DLRK3588 开发板 + 板载扬声器（喇叭）

- 系统：Ubuntu 24.04（从 Buildroot 移植过来的 rootfs）

- 初始问题：aplay 执行成功但没有声音

- 对比对象：Buildroot 系统上喇叭正常

## 二、所有排查操作（按时间顺序，每一步都包含命令、原理、结果）

### 第 1 步：用户最初尝试播放音频

**命令验证：**

```
aplay /usr/share/sounds/alsa/Front_Center.wav
aplay -D plughw:1,0 /usr/share/sounds/alsa/Front_Center.wav
```

**结果：**

- aplay命令执行成功（无错误输出）

- 喇叭没有发出任何声音

**初步判断：** 音频播放命令已成功执行，说明软件层面正常，但声音没有到喇叭。

### 第 2 步：用户尝试使用 GStreamer 播放

**命令验证：**

```
gst-launch-1.0 playbin uri=file:///userdata/piano2-CoolEdit.mp3
gst-launch-1.0 uridecodebin uri=file:///userdata/piano2-CoolEdit.mp3 ! audioconvert ! audioresample ! alsasink device=plughw:1,0
```

**结果：**

- 管道执行成功，显示 Got EOS

- 喇叭没有任何声音

**初步判断：** 多个播放工具都成功执行但无声，问题不在播放工具。

### 第 3 步：检查声卡是否被系统识别

**命令：**

```
cat /proc/asound/cards
```

**结果：**

```
1 [rockchipes8388 ]: rockchip-es8388 - rockchip-es8388
```

**结论：** 声卡 rockchipes8388 已正确注册。

### 第 4 步：检查播放设备节点是否存在

**命令：**

```
ls -l /dev/snd/
```

**结果：**

```
crw-rw---- 1 root audio 116, 4 Aug 7 11:10 pcmC1D0p
```

**结论：** 播放设备节点 pcmC1D0p 存在。

### 第 5 步：使用 speaker-test 测试 ALSA 层

**命令：**

```
speaker-test -c 2 -D plughw:1,0 -t sine -f 1000
```

**结果：**

```
Playback device is plughw:1,0
Stream parameters are 48000Hz, S16_LE, 2 channels
Sine wave rate is 1000.0000Hz
...
0 - Front Left
1 - Front Right
Time per period = 2.750624
```

**结论：** speaker-test 成功运行，显示 Front Left 和 Front Right 交替输出，说明音频数据已成功写入 ALSA DMA 缓冲区。

### 第 6 步：检查 alsamixer 中的音量设置

**命令：**

```
alsamixer -c 1
```

**命令验证：** 在 alsamixer 界面中查看 Headphone、Speaker、PCM 等通道的音量。

**结果：** 音量通道显示正常，但喇叭仍然无声。

### 第 7 步：列出声卡所有控制项

**命令：**

```
amixer -c 1 scontrols
```

**结果（部分）：**

```
Simple mixer control 'Headphone',0
Simple mixer control 'Speaker',0
Simple mixer control 'PCM',0
Simple mixer control 'hp switch',0
Simple mixer control 'spk switch',0
Simple mixer control 'Mono Mux',0
Simple mixer control 'Left Mixer Left Playback Switch',0
Simple mixer control 'Right Mixer Right Playback Switch',0
Simple mixer control 'OUT1 Switch',0
Simple mixer control 'OUT2 Switch',0
Simple mixer control 'Output 1 Playback Volume',0
Simple mixer control 'Output 2 Playback Volume',0
```

### 第 8 步：导出所有控制项的当前状态

**命令：**

```
amixer -c 1 contents > /tmp/amixer.txt
cat /tmp/amixer.txt
```

**发现的关键信息：**

```
numid=40,iface=MIXER,name='hp switch' - values=off
numid=39,iface=MIXER,name='spk switch' - values=off
numid=55,iface=MIXER,name='OUT1 Switch' - values=off
numid=56,iface=MIXER,name='OUT2 Switch' - values=off
```

**结论：** 有多个开关处于关闭状态。

### 第 9 步：开启发现的所有关闭的开关

**命令：**

```
amixer -c 1 sset 'hp switch' on
amixer -c 1 sset 'spk switch' on
amixer -c 1 sset 'Headphone' on
amixer -c 1 sset 'Speaker' on
amixer -c 1 cset "name='Left Mixer Left Playback Switch'" on
amixer -c 1 cset "name='Right Mixer Right Playback Switch'" on
amixer -c 1 cset "name='PCM Playback Volume'" 192
amixer -c 1 cset "name='Output 1 Playback Volume'" 33
amixer -c 1 cset "name='Output 2 Playback Volume'" 33
amixer -c 1 cset "name='Mono Mux'" 0
```

**结果：** 所有命令执行成功，但喇叭仍然无声。

### 第 10 步：检查 ALSA UCM 配置是否加载

**命令：**

```
alsaucm -c rockchipes8388 dump text
```

**结果：**

```
ALSA lib main.c:1554:(snd_use_case_mgr_open) error: failed to import rockchipes8388 use case configuration -2
alsaucm: error failed to open sound card rockchipes8388: No such file or directory
```

**结论：** UCM 配置未加载。UCM 中的 BootSequence（包含音频初始化命令）没有执行。

### 第 11 步：检查 UCM 目录是否存在

**命令：**

```
ls -la /usr/share/alsa/ucm2/Rockchip/
```

**结果：**

```
drwxr-xr-x 2 root root 4096 Aug 7 11:10 rk3588-es8316
```

**结论：** UCM 配置文件存在（rk3588-es8316 目录），但声卡名称 rockchipes8388 与 UCM 目录名 rk3588-es8316 不匹配。

### 第 12 步：获取声卡的驱动名称

**命令：**

```
ls -l /sys/class/sound/card1/device/driver
```

**结果：**

```
lrwxrwxrwx 1 root root 0 Aug 7 10:29 /sys/class/sound/card1/device/driver -> ../../../bus/platform/drivers/rk-multicodecs
```

**结论：** 驱动名称是 rk-multicodecs。ALSA 查找 UCM 配置的依据是驱动名称，不是声卡名称。

### 第 13 步：尝试用驱动名称加载 UCM

**命令：**

```
alsaucm -c rk-multicodecs dump text
```

**结果：**

```
ALSA lib main.c:1554:(snd_use_case_mgr_open) error: failed to import rk-multicodecs use case configuration -2
alsaucm: error failed to open sound card rk-multicodecs: No such file or directory
```

**结论：** 用驱动名称加载 UCM 也失败。

### 第 14 步：尝试创建 conf.d 映射文件

**命令：**

```
mkdir -p /usr/share/alsa/ucm2/conf.d/rk-multicodecs
cat > /usr/share/alsa/ucm2/conf.d/rk-multicodecs/rk-multicodecs.conf << 'EOF'
SectionUseCase."HiFi" {
    File "/Rockchip/rk3588-es8316/HiFi.conf"
    Comment "Play HiFi quality Music"
}
EOF
```

**结果：** 映射文件创建成功，但 alsaucm 仍然报错 -2。

### 第 15 步：尝试创建符号链接

**命令：**

```
ln -sf /usr/share/alsa/ucm2/Rockchip/rk3588-es8316 /usr/share/alsa/ucm2/Rockchip/rockchipes8388
```

**结果：** 符号链接创建成功，但 alsaucm 仍然报错。

### 第 16 步：用 strace 追踪 UCM 查找路径（未执行，但可做）

**命令（建议但未执行）：**

```
strace -e trace=file alsaucm -c rk-multicodecs dump text 2>&1 | grep ucm2
```

**说明：** 该命令可显示 ALSA 实际查找的路径，帮助定位 UCM 加载失败原因。

### 第 17 步：检查 GPIO 功放使能状态

**命令：**

```
cat /sys/kernel/debug/gpio | grep -i spk
```

**结果：**

```
gpio-136 (|spk-con) out hi
```

**结论：** 功放使能 GPIO 已经处于高电平状态。功放已使能。

### 第 18 步：尝试导出 GPIO 136

**命令：**

```
echo 136 > /sys/class/gpio/export
```

**结果：**

```
cat: /sys/class/gpio/gpio136/value: No such file or directory
```

**结论：** GPIO 136 已被内核占用，无法从用户空间导出。这说明 GPIO 由内核驱动管理，功能正常。

### 第 19 步：重新检查所有输出相关开关

**命令：**

```
amixer -c 1 controls | grep -i "output\|out"
```

**结果：**

```
numid=55,iface=MIXER,name='OUT1 Switch'
numid=56,iface=MIXER,name='OUT2 Switch'
numid=34,iface=MIXER,name='Output 1 Playback Volume'
numid=35,iface=MIXER,name='Output 2 Playback Volume'
```

### 第 20 步：检查 OUT1 Switch 和 OUT2 Switch 的具体状态

**命令：**

```
amixer -c 1 cget "name='OUT1 Switch'"
amixer -c 1 cget "name='OUT2 Switch'"
```

**结果：**

```
numid=55,iface=MIXER,name='OUT1 Switch'
  ; type=BOOLEAN,access=rw------,values=1
  : values=off

numid=56,iface=MIXER,name='OUT2 Switch'
  ; type=BOOLEAN,access=rw------,values=1
  : values=off
```

**结论：** OUT1 Switch 和 OUT2 Switch 处于关闭状态。这两个开关是 ES8388 的物理输出级开关，位于信号路径的最后一级。

### 第 21 步：开启 OUT1 Switch 和 OUT2 Switch（最终成功）

**命令：**

```
amixer -c 1 cset "name='OUT1 Switch'" on
amixer -c 1 cset "name='OUT2 Switch'" on
```

**结果：**

```
numid=55,iface=MIXER,name='OUT1 Switch'
  ; type=BOOLEAN,access=rw------,values=1
  : values=on

numid=56,iface=MIXER,name='OUT2 Switch'
  ; type=BOOLEAN,access=rw------,values=1
  : values=on
```

### 第 22 步：验证喇叭是否出声

**命令：**

```
speaker-test -c 2 -D plughw:1,0 -t sine -f 1000
```

**结果：** ✅ 喇叭正常发声！

## 三、所有错误推断（按时间顺序）

| 序号 | 错误推断 | 被推翻的证据 | 
| -- | -- | -- |
| 1 | “播放工具或管道配置有问题” | gst-launch 和 aplay 都成功执行，说明工具正常 | 
| 2 | “ALSA 音量设置太低或被静音” | alsamixer 显示音量正常，开启后仍无声 | 
| 3 | “UCM 配置是必须的，少了文件” | 手动 amixer 可以完全控制音频通路 | 
| 4 | “驱动名称是 rockchipes8388” | ls -l /sys/class/sound/card1/device/driver 显示是 rk-multicodecs | 
| 5 | “UCM 符号链接可以解决问题” | 符号链接创建后 alsaucm 仍报错 | 
| 6 | “conf.d 映射文件可以解决问题” | 映射文件创建后 alsaucm 仍报错 | 
| 7 | “功放 GPIO 未使能” | gpio-136 spk-con out hi 显示已使能 | 
| 8 | “用户空间需要导出 GPIO” | GPIO 已被内核占用，这是正常状态 | 
| 9 | “MCLK 门控关闭导致无声” | clk_summary 显示 MCLK 已开启 | 
| 10 | “内核版本或设备树不匹配” | 声卡已正确注册，问题不在内核 | 
| 11 | “hp switch 或 spk switch 是问题根源” | 开启后仍无声 | 
| 12 | “混音器开关（Left Mixer...）是问题根源” | 开启后仍无声 | 


## 四、正确的排查路径（最终找到问题的方法）

| 步骤 | 操作 | 发现 | 
| -- | -- | -- |
| 1 | amixer -c 1 controls | grep -i "output|out" | 发现 OUT1 Switch 和 OUT2 Switch | 
| 2 | amixer -c 1 cget "name='OUT1 Switch'" | 状态为 off | 
| 3 | amixer -c 1 cset "name='OUT1 Switch'" on | 开启成功 | 
| 4 | speaker-test | 喇叭出声 | 


## 五、ES8388 信号路径与问题点

```
数字数据 (DMA)
    │
    ▼
I2S 接口 (接收 PCM 数据)
    │
    ▼
DAC (数模转换)          ← ✅ `speaker-test` 验证数据已到达
    │
    ▼
DAC 输出音量控制
    │
    ▼
Left/Right Mixer        ← ✅ 已开启 (`Left Mixer Left Playback Switch`)
    │
    ▼
OUT1/OUT2 输出级开关    ← ❌ **问题点**，默认关闭
    │                    ← ✅ 开启后信号通过
    ▼
物理输出引脚 (OUT1/OUT2)
    │
    ▼
外部功放 (PA)           ← ✅ GPIO 136 已使能 (`spk-con out hi`)
    │
    ▼
喇叭                    ← ✅ 最终正常发声
```

## 六、为什么 Buildroot 正常而 Ubuntu 无声？

Buildroot 的 UCM 配置文件 HiFi.conf 中包含 BootSequence，系统启动时自动执行：

```
BootSequence [
    cset "name='OUT1 Switch'" on
    cset "name='OUT2 Switch'" on
    ...
]
```

Ubuntu 上 UCM 未加载（alsaucm 报错 -2），导致 OUT1 Switch 和 OUT2 Switch 保持默认关闭状态，信号被截断在芯片内部。

## 七、最终有效的命令（只有这两条是必要的）

```
amixer -c 1 cset "name='OUT1 Switch'" on
amixer -c 1 cset "name='OUT2 Switch'" on
```

## 八、所有之前执行但最终被证明不必要/无效的操作

| 操作 | 为什么无效 | 
| -- | -- |
| 设置 hp switch on | 控制耳机，与喇叭无关 | 
| 设置 spk switch on | ES8388 内部开关，不影响 OUT1/OUT2 | 
| 设置 Headphone 音量 | 控制耳机音量，与喇叭无关 | 
| 设置 Speaker 音量 | ES8388 内部音量控制，不是根本原因 | 
| 设置 PCM Playback Volume 192 | 音量，不是开关问题 | 
| 设置 Output 1/2 Playback Volume 33 | 音量，不是开关问题 | 
| 设置 Mono Mux Stereo | 模式选择，不是静音原因 | 
| 设置 Left/Right Mixer... on | 混音器开关，不是最后一级 | 
| 创建 UCM 符号链接 | UCM 加载机制本身有问题 | 
| 创建 conf.d 映射文件 | UCM 加载机制本身有问题 | 
| 检查 MCLK 时钟 | 时钟正常工作 | 
| 检查 GPIO 136 | 功放已使能 | 
| 检查设备树 | 设备树正常 | 
| 安装 GStreamer 插件 | 播放工具正常 | 
| 更新内核 | 内核不是根本原因 | 


## 九、永久生效配置

在 /etc/rc.local 的 exit 0 之前添加：

```
# OUT1/OUT2 开关开启
amixer -c 1 cset "name='OUT1 Switch'" on
amixer -c 1 cset "name='OUT2 Switch'" on

# 设置 ALSA 默认声卡为 card 1 (ES8388)
cat > /etc/asound.conf << 'EOF'
pcm.!default {
    type hw
    card 1
}
ctl.!default {
    type hw
    card 1
}
EOF

```

## 十、最终结论

| 项目 | 内容 | 
| -- | -- |
| 问题根源 | ES8388 芯片内部 OUT1 Switch 和 OUT2 Switch 默认关闭 | 
| 为什么 speaker-test 正常但无声 | 数据到达 DAC，但被 OUT1/OUT2 开关截断 | 
| 为什么 Buildroot 正常 | UCM 的 BootSequence 自动开启了 OUT1/OUT2 开关 | 
| 为什么 Ubuntu 无声 | UCM 未加载，OUT1/OUT2 开关保持默认关闭 | 
| 最终解决 | 两条 amixer 命令开启 OUT1 Switch 和 OUT2 Switch | 
| 所有其他操作 | 排查过程中的无效尝试 | 
